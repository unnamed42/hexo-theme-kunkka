#toc {
    line-height: 1em;
    position: fixed;
    right: 5%;
    width: 20%;
    height: 90%;
    top: 90px;
    overflow: hidden;
    .toc-text {white-space: nowrap;}
    for i in (1..6) {
        .toc-level-{i} .toc-text {margin-left: i em;}
    }
    .toc-link {
        color: #999;
        &:hover {
            color: $color-theme;
            border-left: 1px solid $color-theme;
            text-decoration: none;
        }
    }
    .toc {
        padding: 0;
        margin: 0.5em;
        line-height: 1.8em;
        overflow: scroll;
        width: 102%;
        height: 101%;
        li {list-style-type: none;}
    }
    .toc-item {
        .toc-child {
            margin-left: 0;
            padding-left: 0;
            if(hexo-config('toc.collapse')){display: none;}
        }
        &.active {
            > .toc-link {
                color: $color-theme;
                border-left: 2px solid $color-theme;
            }
            .toc-child {if(hexo-config('toc.collapse')){display: block;}}
        }
    }
}
